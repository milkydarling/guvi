#include<stdio.h>
#include<string.h>
void main()
{
        char s[10];
        int i;
        scanf("%s",&s[i]);
        int l=strlen(s);
        for(i=0;i<l;i++)
        {
                s[i]=s[i]+3;
                printf("%c",s[i]);
        }
}
